(** Add some input_ methods missing in Pervasives *)

val input_i32le : in_channel -> int

val input_i32be : in_channel -> int

val input_i64le : in_channel -> int64

val input_d64le : in_channel -> float
